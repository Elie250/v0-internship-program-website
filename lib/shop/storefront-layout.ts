/** Shared public storefront width — full viewport with modest padding. Not a dashboard max-width. */
export const STOREFRONT_GUTTER = 'w-full px-3 sm:px-4 lg:px-6'

/** Readable inner width for cart, checkout, tracking, and similar customer forms. */
export const STOREFRONT_FORM = 'mx-auto w-full max-w-5xl'
export const STOREFRONT_NARROW = 'mx-auto w-full max-w-2xl'
